export function Header() {
  return (
    <header className="Header flex items-center justify-between px-10 py-4 bg-surface-default">
      <span className="Header-Wordmark font-display text-2xl tracking-wide text-text-default">
        STRIDE
      </span>
    </header>
  )
}
